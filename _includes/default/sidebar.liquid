<aside class="sidebar">
  <ul>
    {% assign menu_items = site.data.menu %}
    {% for item in menu_items %}
      <li><a href="{{ item.url }}">{{ item.title }}</a></li>
    {% endfor %}
  </ul>
</aside>